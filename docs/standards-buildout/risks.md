# LEXA Hub Standards Build-Out — Risk Register (top 10)

Companion to architecture.md / work-packages.md. Ordered by (impact × likelihood).

| # | Risk | Why it's real | Mitigation |
|---|---|---|---|
| R1 | **GFEMS aggregation semantics rejected by lab/utility** (DERType choice, summed ratings, modesSupported mask read as wrong for one EndDevice behind a PCC) | CSIP delegates aggregate semantics to utility handbooks; no normative sum rule exists; CORE-014 is operator-judged | Per-field config overrides (`der_type`, rating overrides); set≤rtg by construction + pinned tests; omission-over-fabrication (G27); PICS declares the mask; dry-run vs gridsim replica of the lab topology BEFORE booking lab time (config freeze makes late fixes a full restart) |
| R2 | **Adv desired-doc schema churn** — D6 shape wrong once real inverters expose vendor quirks (adopt handshake variants, curve point limits, 12x-only fleets) | AdptCrvRslt semantics are known vendor-quirky; the doc is a retained wire contract consumed by a single-writer reconciler | Doc is a NEW family (V=1, additive evolution cheap); adopt_state=unsupported is a first-class outcome, not an error; shadow mode ships first (`reconciler.adv:"shadow"`); readback-verify catches lie-and-ACK; schema review gate before WP-9 merges |
| R3 | **Dual-stack OCPP regressions on the hardened 2.0.1 path** (shared bridge state, shared reconcile shell) | mqttBridge state machine is bench-hardened; 1.6 session lifecycle (Start/StopTransaction) maps imperfectly onto TransactionEvent shapes | `port_16=0` default (1.6 fully off in product until flipped); proto tag on stationState; 2.0.1 test suite must pass byte-identical (explicit acceptance gate); 1.6 feature depth capped at smart-charging minimum per expert verdict |
| R4 | **ACL drift silently dropping new topics** — 9 new topic rows + 1 new broker user; a missed grant drops CannotComply-class evidence with no error (RSK-09 pattern) | Proven live: lexa-api lacks the certstatus read grant TODAY (mosquitto-lexa.acl:188-245 vs cmd/api's subscribe) — found during this design's verification | Every WP adding a subscribe/publish ships its ACL row in the SAME commit; WP-17 re-derives the full matrix from call sites; bench journal check "connected (broker user=...)" + subscription-refused scan added to the validation queue |
| R5 | **Response-code remap breaks gridsim/bench mid-transition** (0xF0 → Table 27) | gridsim asserts 0xF0 today; Mayhem scenarios key on CannotComply flow | `legacy_cannotcomply_code` flag; bench configs flip to legacy=true OR gridsim updated in the SAME session (MTR-4 paired-change discipline); default-standard only ships with the paired harness change merged |
| R6 | **PIN-freeze bricks commissioning** (fail-closed posture stops adoption/egress on a provisioning typo at a new site) | D4 deliberately freezes on mismatch; out-of-band LFDI pre-provisioning mistakes are common day-one events | `registration_pin=0` shipped default (check off + WARN); freeze is loud (gauge, /status pin_ok, slog error) and self-heals on next walk; runbook entry; freeze never drops an adopted control (LKG hold) |
| R7 | **Bus-lane serialization becomes the schedule bottleneck** (6 WPs touch messages/desired/topics/envelope.go) | Single-owner files; parallel agents otherwise idle waiting on merges | New types in new files (only constants/SupportedV arms serialize); one agent owns the lane end-to-end; lane slots pre-assigned in WP order; conflicts are additive-append (low merge cost) |
| R8 | **Curve execution diverges on real hardware vs sim** — adopt handshake passes in sim, real inverter ignores/mangles curves; bench currently occupied so soak is deferred | The whole program's "trust measurement" lesson says ACK≠effect; we cannot bench-validate now | Readback-after-adopt is mandatory (never trust AdptCrvRslt alone); one-sided convergence under volt-watt/droop; active flip is bench-gated (explicit queue item, WP-17f); until then shadow verdicts accumulate evidence at zero write risk |
| R9 | **DER*/LogEvent reporting against a server that doesn't offer the resources** (404s, strict WADL servers rejecting PUT bodies, postRate misreads) → log spam or reporting loops | Real head-ends vary; CSIP Table 8 duty is "if the server makes them available" | Per-resource 404/405 skip-once-and-log; report cadence bounded by pollRate/change-detect; `der_report:false` escape hatch; unencrypted-log dry-run vs gridsim (which needs PUT endpoints — paired harness work item flagged in WP-17) |
| R10 | **lexa-openadr scope creep + auth fragility** (2.0b pull, subscriptions/webhooks, token-refresh edge cases, second external TLS surface) | OpenADR programs love bespoke requirements; OAuth failures are silent-degradation shaped | CP-profile-only gate; poll-only VEN (no inbound listener, no MQTT notifier v1); build trigger = ONE named program (expert #10); token flow unit-tested against httptest VTN; retained `lexa/openadr/status` + watchdog make silent degradation visible; uncommissioned idle default |

Watch-list (below top-10): tick-budget pressure from new hub publishers (mitigated: async+dedupe,
watch `lexa_hub_tick_overruns_total`); flash-budget from new edge logs (rate floors, WP-17e
review); EV clamp accidentally removed during future V2X work (clamp is a single greppable seam
with a pinned test); cert-campaign config freeze vs staged flags (land+soak+freeze sequencing in
WP-16 notes).
